#!/bin/sh
# launchctl setenv PATH "/bin:/sbin:/usr/bin:/usr/sbin" # Your path, maybe the same as $PATH in a shell
# launchctl setenv ORG_HOME "/path/to/org" # Your path to the orgmode installation



